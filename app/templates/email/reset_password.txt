{{ user.username }},

To reset your password click the following link:

{{ url_for('reest_password', token=token, _external=True) }}

If you have not requested a password reset simply ignore this message.


The FencingTournamentTool Team
